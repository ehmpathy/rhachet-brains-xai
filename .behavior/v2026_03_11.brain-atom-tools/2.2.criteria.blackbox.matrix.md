# blackbox criteria matrix: xai brain atom tool use

## matrix.1: tool invocation outcomes

| ind: output_schema | ind: brain_action | dep: result.calls | dep: result.output | dep: result.metrics |
|--------------------|-------------------|-------------------|--------------------|--------------------|
| with response_format | calls tool | has tools[] | null | tracked |
| with response_format | answers directly | null | conforms to schema | tracked |
| without response_format | calls tool | has tools[] | null | tracked |
| without response_format | answers directly | null | has value | tracked |

**gaps**: none — all 4 combinations covered

---

## matrix.2: tool call structure

| ind: tool_count | dep: calls.tools.length | dep: each has exid | dep: each has slug | dep: each has input |
|-----------------|-------------------------|--------------------|--------------------|---------------------|
| single tool | 1 | ✓ | ✓ | ✓ |
| multiple tools (parallel) | > 1 | ✓ (distinct) | ✓ | ✓ |

**gaps**: none — both counts covered

---

## matrix.3: error boundaries

| ind: error_source | dep: when_detected | dep: error_context |
|-------------------|--------------------|--------------------|
| invalid tool schema | at ask time | schema validation |
| malformed provider response | at parse time | response details |

**gaps**: none — both error sources covered

---

## decomposition analysis

**independent dimensions**: 3 (output_schema, brain_action, tool_count)

this is within acceptable bounds (< 4 dimensions). no decomposition needed.

**note**: tool_count is orthogonal to output_schema — they can be tested independently. the matrices above reflect this separation.
