# roadmap: xai brain atom tool use

## phases

### phase 0: read context

before implementation, read:
- .behavior/v2026_03_11.brain-atom-tools/3.3.1.blueprint.product.v1.i1.md (blueprint)
- .behavior/v2026_03_11.brain-atom-tools/3.1.3.research.internal.product.code.prod._.v1.i1.md (extant prod code)
- .behavior/v2026_03_11.brain-atom-tools/3.1.3.research.internal.product.code.test._.v1.i1.md (extant test code)

### phase 1: extend genBrainAtom.ts

- [x] 1.1: add tool conversion logic
  - convert `plugs.tools` to openai tools format
  - use `z.toJSONSchema()` for schema conversion
  - acceptance: compiles with valid tool definitions

- [x] 1.2: add prompt type check
  - if prompt is string: add as user message (extant behavior)
  - if prompt is `BrainPlugToolExecution[]`: convert to tool messages
  - acceptance: both prompt types handled

- [x] 1.3: add tool_calls response parse
  - check for `tool_calls` in response
  - if present: extract invocations, set output null
  - if absent: parse content as before
  - acceptance: tool calls extracted correctly

- [x] 1.4: update return type
  - populate `calls.tools` with `BrainPlugToolInvocation[]`
  - ensure `output` is null when tools called
  - acceptance: return shape matches rhachet contract

### phase 2: add integration tests

before tests, read:
- .behavior/v2026_03_11.brain-atom-tools/2.1.criteria.blackbox.md (criteria)

- [x] 2.1: add case 4 - tool invocation with output schema
  - scenario: ask with tools plugged + structured output
  - assertions: calls.tools populated, output null, slug/input/exid present
  - acceptance: test passes

- [x] 2.2: add case 5 - tool invocation without output schema
  - scenario: ask with tools plugged, no response_format
  - assertions: calls.tools populated, output null
  - acceptance: test passes

- [x] 2.3: add case 6 - brain answers directly
  - scenario: ask with tools plugged but prompt answerable directly
  - assertions: calls null, output populated
  - acceptance: test passes

- [x] 2.4: add case 7 - metrics with tools
  - scenario: verify metrics present when tools used
  - assertions: tokens, cost, time present
  - acceptance: test passes

- [x] 2.5: add case 8 - tool continuation
  - scenario: feed BrainPlugToolExecution[] back to continue episode
  - assertions: final output populated after tool result
  - acceptance: test passes

### phase 3: verify

- [x] 3.1: run all tests
  - `npm run test:integration -- genBrainAtom`
  - acceptance: all tests pass (44 passed)

- [x] 3.2: run type check
  - `npm run test:types`
  - acceptance: no type errors

---

## dependencies

```
phase 0 (read) → phase 1 (implement) → phase 2 (test) → phase 3 (verify)
```

phase 2 depends on phase 1 completion.
phase 3 depends on phases 1 and 2 completion.
