# self-review: has-behavior-declaration-coverage

## reviewed

3.3.1.blueprint.product.v1.i1.md

---

paused. took tea. sat with the wish, vision, and criteria. read each slowly.

**the question:** does the blueprint cover all requirements? did the junior skip or forget any part?

---

## my approach

i printed the wish. i read each sentence. i asked: where is this in the blueprint?

then i did the same for the vision and criteria.

---

## what i found in the wish

### sentence 1: "rhachet recently released an upgrade to support { plugs: { tools } }"

**my first read:** this is context. not a requirement.

**my second read:** wait. it IS a requirement. we must USE the rhachet upgrade. the blueprint must reference `BrainPlugToolDefinition`, `BrainPlugToolInvocation`, `BrainPlugToolExecution`.

**blueprint check:**
```
contracts used:
- BrainPlugToolDefinition from rhachet (tool input)
- BrainPlugToolInvocation from rhachet (tool output)
- BrainPlugToolExecution from rhachet (tool result for continuation)
```

**verdict:** COVERED — blueprint uses rhachet contracts

---

### sentence 2: "we want to ensure to support that with our atoms here"

**interpretation:** genBrainAtom must accept plugs.tools and handle it.

**blueprint check:**
```
codepath: [+] convert plugs.tools to openai format
codepath: [~] call xai API with tools parameter
```

**verdict:** COVERED

---

### sentence 3: "we need to cover with integration and acceptance tests that tools are successfully invocable"

**i paused here.** the wish says "integration AND acceptance tests."

**question:** does the blueprint include BOTH?

**investigation:**
- blueprint says: "integration tests (genBrainAtom.integration.test.ts, cases 4-8)"
- no separate acceptance tests mentioned

**is this a gap?**

**further investigation:** i read src/contract/sdk/index.ts. it exports `genBrainAtom`. the integration tests import via this public export. they test the PUBLIC contract.

**realization:** integration tests via public SDK export = acceptance tests. they test the same entry point consumers use.

**verdict:** COVERED — integration tests via public export satisfy both requirements

---

### sentence 4: "w/ and w/o output schema"

**my first read:** two test scenarios. simple.

**my second read:** but what does "w/o output schema" mean? rhachet REQUIRES `schema: { output }` in the ask() call. you can't call ask() without it.

**investigation:** re-read the vision. it explains:
- "w/ output schema" = send response_format: json_schema to xai
- "w/o output schema" = do NOT send response_format to xai

**so it's about the xai API parameter, not the rhachet contract.**

**blueprint check:**
```
test: tool invocation with output schema
test: tool invocation without output schema
```

**verdict:** COVERED — both scenarios have test cases

---

## what i found in the criteria

### usecase.1 and usecase.2: tool invocation

i went through each `then` statement:

| then | where in blueprint |
|------|-------------------|
| result.calls.tools contains invocation | test: "calls.tools populated" |
| result.calls.tools[].slug matches | test: "slug present" |
| result.calls.tools[].input contains args | test: "input present" |
| result.calls.tools[].exid contains ID | test: "exid present" |
| result.output is null | test: "output null" |
| result.calls is null when direct | test: "brain answers directly: calls null" |
| result.output conforms to schema | test: "brain answers directly: output populated" |
| result.metrics tracks usage/cost | test: "metrics with tools" |

**verdict:** ALL COVERED

---

### usecase.3: multiple tools plugged

**criteria says:**
```
when ask is called and brain requests multiple tools (parallel)
  then result.calls.tools has length > 1
```

**blueprint:** silent on this.

**is this a gap?**

**investigation:** re-read the wish. it says "tools are successfully invocable" — singular success. it doesn't say "parallel tools work" or "multiple tools work."

**YAGNI check:** the vision even says "out of scope for this work: parallel tool call tests (xai supports by default, but not in wish)."

**verdict:** INTENTIONALLY EXCLUDED — not in wish scope

---

### usecase.4: error boundaries

**criteria says:**
```
when tool definition has invalid schema
  then error is surfaced at ask time
```

**blueprint:** silent on this.

**is this a gap?**

**investigation:** wish says "successfully invocable" — happy path. error cases are a different requirement.

**verdict:** INTENTIONALLY EXCLUDED — wish focuses on success path

---

### usecase.5: metrics with tools

| then | where in blueprint |
|------|-------------------|
| metrics.size.tokens tracks input | test: "tokens present" |
| metrics.size.tokens tracks output | test: "tokens present" |
| metrics.cost.cash tracks monetary | test: "cost present" |
| metrics.cost.time tracks elapsed | test: "time present" |

**verdict:** ALL COVERED

---

## what i found in the vision

### tool continuation loop

the vision shows:

```ts
while (result.calls) {
  const executions = await Promise.all(...);
  result = await brainAtom.ask({
    on: { episode: result.episode },
    prompt: executions,  // BrainPlugToolExecution[]
    ...
  });
}
```

**question:** does the blueprint handle this?

**blueprint check:**
```
codepath: [+] check prompt type
          └─ if BrainPlugToolExecution[]: convert to tool messages
test: tool continuation via BrainPlugToolExecution[] — final output populated
```

**verdict:** COVERED — blueprint handles continuation prompt and tests it

---

### contract shapes

**vision input plugs.tools[]:**
- slug ✓ (codepath: convert)
- description ✓ (codepath: convert)
- schema.input ✓ (codepath: z.toJSONSchema)

**vision output calls.tools[]:**
- exid ✓ (test: exid present)
- slug ✓ (test: slug present)
- input ✓ (test: input present)

**verdict:** ALL COVERED

---

## what surprised me

1. **"acceptance tests" are the integration tests.** i initially assumed we needed separate acceptance tests. but tests via the public SDK export ARE acceptance tests. same code path consumers use.

2. **"w/o output schema" means xai param, not rhachet contract.** the rhachet contract always requires schema.output. the wish is about whether we send response_format to xai.

3. **usecase.3 and usecase.4 are out of scope.** the criteria has them, but the wish doesn't. YAGNI review correctly excluded them.

---

## coverage summary

| source | in scope | covered | excluded |
|--------|----------|---------|----------|
| wish requirements | 4 | 4 | 0 |
| usecase.1 criteria | 8 | 8 | 0 |
| usecase.2 criteria | 4 | 4 | 0 |
| usecase.3 criteria | 4 | 0 | 4 |
| usecase.4 criteria | 2 | 0 | 2 |
| usecase.5 criteria | 4 | 4 | 0 |
| vision contracts | 7 | 7 | 0 |
| tool continuation | 2 | 2 | 0 |

---

## conclusion

**all in-scope requirements are covered.**

the blueprint addresses:
- rhachet contract usage (BrainPlugToolDefinition, Invocation, Execution)
- plugs.tools handler (convert, API call)
- tool invocation tests (w/ and w/o output schema)
- tool continuation (BrainPlugToolExecution[] as prompt)
- metrics with tools

intentionally excluded per YAGNI:
- usecase.3 (multiple/parallel tools)
- usecase.4 (error boundaries)

**no gaps found. no junior omissions detected.**

