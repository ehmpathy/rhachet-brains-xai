# self-review: has-consistent-mechanisms

## reviewed

3.3.1.blueprint.product.v1.i1.md

---

paused. took tea. re-read the blueprint with fresh eyes.

**the question:** does the blueprint introduce mechanisms that duplicate extant functionality?

**my approach:** trace each proposed mechanism back to a source — either extant code to reuse, or evidence that no extant alternative exists.

---

## what i searched

### 1. local codebase

```
src/
├── .test/assets/dir.ts                         # test asset path
├── index.ts                                    # re-export
├── contract/sdk/index.ts                       # SDK exports
├── contract/sdk/index.unit.test.ts
└── domain.operations/atom/
    ├── BrainAtom.config.ts                     # model configs
    ├── genBrainAtom.ts                         # the function we extend
    └── genBrainAtom.integration.test.ts        # tests we add to
```

**key insight:** this is a tiny codebase. there are no utility modules, no helper files, no shared converters. genBrainAtom.ts IS the codebase. any "duplication" would be within this single file.

### 2. rhachet package

searched `node_modules/rhachet/dist/` for tool conversion utilities:
- `castToolsToOpenai` — not found
- `convertToolDefinitions` — not found
- `mapToolsForProvider` — not found

**found instead:**
- `BrainPlugToolDefinition` (domain object)
- `BrainPlugToolInvocation` (domain object)
- `BrainPlugToolExecution` (domain object)
- `asBrainPlugToolDict` (caller utility for O(1) lookup)
- `genBrainPlugToolDeclaration` (factory for creating tools)

**key insight:** rhachet intentionally does NOT provide wire format converters. why? because each provider (xai, openai, anthropic) has its own API quirks. provider implementations own their conversion logic.

### 3. openai SDK

the SDK provides TypeScript types (`ChatCompletionTool`, `ChatCompletionToolMessageParam`) but NO conversion utilities. you construct the objects yourself.

---

## mechanism-by-mechanism trace

### mechanism: tool definition → openai format

**blueprint says:**
```
convert plugs.tools to openai format
```

**what i searched for:**
- any function that takes BrainPlugToolDefinition and returns ChatCompletionTool
- any shared converter in rhachet
- any pattern in other rhachet-brains-* repos

**what i found:** none. this conversion does not exist anywhere.

**why this makes sense:** the conversion is trivial (3 field maps). extracting to a utility would be premature. if this repo later needs to convert tools in multiple places, THEN extract. per WET principle, wait for 3+ usages.

**verdict:** NEW mechanism. no extant alternative.

---

### mechanism: z.toJSONSchema()

**blueprint says:** use z.toJSONSchema() for tool.schema.input

**extant usage in genBrainAtom.ts:88:**
```ts
const jsonSchema = z.toJSONSchema(askInput.schema.output);
```

**verification:** same zod import, same conversion function, same pattern. the blueprint reuses the extant approach for schema conversion.

**verdict:** REUSE. no new mechanism introduced.

---

### mechanism: prompt type check

**blueprint says:**
```
check prompt type
├─ if string: add as user message
└─ if BrainPlugToolExecution[]: convert to tool messages
```

**extant code (line 85):**
```ts
messages.push({ role: 'user', content: askInput.prompt });
```

**what changes:** we add `Array.isArray(prompt)` check before this line. if array, we convert to tool messages instead.

**is this a new mechanism?** yes, but it's a branch extension, not a new utility. the extant message-building logic stays intact. we add a conditional path for the new input type.

**could we use an extant component?** no. rhachet's type system (`AsBrainPromptFor<TPlugs>`) handles the type narrowing, but the runtime check must live here.

**verdict:** EXTEND. branch extension to extant logic.

---

### mechanism: tool_calls response extraction

**blueprint says:**
```
check for tool_calls in response
├─ if tool_calls: extract invocations, set output null
└─ if no tool_calls: parse content as before
```

**extant code (lines 104-109):**
```ts
const content = response.choices[0]?.message?.content ?? '';
const parsed = JSON.parse(content);
const output = askInput.schema.output.parse(parsed);
```

**what changes:** check for `response.choices[0]?.message?.tool_calls` first. if present, extract invocations. if absent, run extant content parse.

**is this a new mechanism?** no. it's the same response handling with an added branch. openai SDK returns tool_calls in the message. we read it and convert to BrainPlugToolInvocation.

**verdict:** EXTEND. branch extension to extant logic.

---

### mechanism: test patterns

**extant test file uses:**
- `given/when/then` from test-fns
- `useThen` for shared API results (avoids duplicate API calls)
- `jest.setTimeout(30000)` for slow API
- real xai API calls (no mocks)
- schema defined upfront: `const outputSchema = z.object({ content: z.string() })`

**blueprint proposes:** same patterns for tool tests. add cases 4-8 to extant file.

**verification:** i read the test file. the patterns are clear:
- `useThen('description', async () => brainAtom.ask(...))` captures result
- subsequent `then(...)` assertions use the captured result
- this avoids calling the API multiple times

**verdict:** FOLLOW. use extant test patterns.

---

### mechanism: metrics calculation

**extant code:**
```ts
const { cash } = calcBrainOutputCost({ for: { tokens: size.tokens }, with: { cost: { cash: config.spec.cost.cash } } });
const metrics = new BrainOutputMetrics({ size, cost: { time: { milliseconds: elapsedMs }, cash } });
```

**tool_calls responses:** same structure. `response.usage` contains token counts regardless of whether tools were called.

**verdict:** REUSE. same metrics logic applies.

---

## what i did NOT find

i specifically looked for and did NOT find:
- a shared tool converter anywhere
- an extant tool_calls handler
- a separate test file for tools
- a utility for prompt type detection

these would be red flags for duplication. their absence confirms the blueprint introduces only what's necessary.

---

## summary

| mechanism | classification | evidence |
|-----------|----------------|----------|
| tool → openai format | NEW | no converter exists in rhachet, openai SDK, or local code |
| z.toJSONSchema() | REUSE | same as line 88 in extant code |
| prompt type check | EXTEND | adds branch to extant message build |
| tool_calls extraction | EXTEND | adds branch to extant response parse |
| test patterns | FOLLOW | same given/when/then, useThen, real API |
| metrics | REUSE | same calcBrainOutputCost, same BrainOutputMetrics |

---

## conclusion

**no duplication found.** every mechanism either reuses extant code, extends extant logic with a branch, or is genuinely new where no alternative exists.

the blueprint is lean: it modifies one file (genBrainAtom.ts) and adds tests to one file (genBrainAtom.integration.test.ts). no new utilities, no new abstractions, no helper modules. this aligns with WET principle — don't extract until you have 3+ usages.

