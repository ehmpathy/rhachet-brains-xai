# self-review: has-questioned-deletables

## reviewed

3.3.1.blueprint.product.v1.i1.md

---

## deletables identified and addressed

### 1. codepath sub-bullets (deleted)

**before:**
```
├─ [+] convert plugs.tools to openai format
│      ├─ map slug → name
│      ├─ map description → description
│      └─ map schema.input → z.toJSONSchema()
```

**after:**
```
├─ [+] convert plugs.tools to openai format
```

**why delete:** blueprint declares WHAT to build, not HOW. implementation details belong in code, not spec.

### 2. unchanged codepath steps (deleted)

**before:** showed `[○] compose system prompt`, `[○] build messages`, `[○] calculate metrics`, `[○] build continuables`

**after:** removed. codepath now shows only changed/added steps.

**why delete:** reader can see extant code for context. blueprint should highlight delta, not restate context.

### 3. "acceptance tests (future)" section (deleted)

**before:** "acceptance tests via published SDK contract (out of scope for this PR, extant pattern)."

**after:** removed entirely.

**why delete:** out of scope means don't mention. the note added noise without actionable value.

### 4. "tool call structure" test row (consolidated)

**before:** separate row: `| tool call structure | slug, input, exid present |`

**after:** merged into `| tool invocation with output schema | calls.tools populated, output null, slug/input/exid present |`

**why consolidate:** same API call, multiple assertions. useThen pattern = one call, many assertions. separate row implied separate call.

---

## what holds and why

### filediff tree holds

- `[~] genBrainAtom.ts` — yes, we modify extant file
- `[~] genBrainAtom.integration.test.ts` — yes, add tool use cases to extant test file

**if we deleted this and had to add it back, would we?** yes. filediff tree is essential for scope clarity.

### codepath tree holds

- `[+] convert plugs.tools` — essential. xai needs openai format tools.
- `[+] check prompt type` — essential. string vs BrainPlugToolExecution[] for continuation.
- `[~] call xai API with tools` — essential. pass tools parameter.
- `[+] check for tool_calls` — essential. branch on response type.
- `[~] return BrainOutput with calls` — essential. surface invocations.

**if we deleted any of these, would we add it back?** yes. each step is necessary for tool use and continuation.

### test coverage holds

| scenario | why keep |
|----------|----------|
| tool invocation with output schema | explicit wish requirement |
| tool invocation without output schema | explicit wish requirement |
| brain answers directly | validates no regression |
| metrics with tools | validates cost tracked |
| tool continuation via BrainPlugToolExecution[] | validates episode continuation with tool results |

**could we reduce to fewer tests?** no. the wish explicitly requires "w/ and w/o output schema" and tool continuation. the direct-answer test validates no false positives. metrics test validates observability.

### contracts section holds

- `BrainPlugToolDefinition` — input contract from rhachet (tool definitions)
- `BrainPlugToolInvocation` — output contract from rhachet (tool calls from brain)
- `BrainPlugToolExecution` — input contract for continuation (tool results)
- `AsBrainPromptFor<TPlugs>` — prompt type conditional on tools plugged
- `BrainOutput` with `calls` — return type
- openai SDK format — wire format for xai

**if we deleted this and had to add it back, would we?** yes. contracts are the interface definitions.

---

## simplest version that works

the simplified blueprint is now ~55 lines (was ~75).

remaining content is essential:
- summary: declares what we build
- filediff: declares scope
- codepath: declares changes
- tests: declares coverage
- contracts: declares interfaces

**no component remains that could be deleted without loss of essential information.**

---

## conclusion

reviewed. found 4 deletables. deleted them. what remains holds.

