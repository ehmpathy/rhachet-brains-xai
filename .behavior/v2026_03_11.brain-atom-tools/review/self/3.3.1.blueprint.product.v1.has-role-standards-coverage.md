# self-review: has-role-standards-coverage

## reviewed

3.3.1.blueprint.product.v1.i1.md

---

paused. took tea. this review asks: are there patterns that should be present but are absent? did we forget error paths, validation, tests, types?

i went through the mechanic briefs systematically.

---

## my approach

1. enumerate each briefs category
2. ask: what pattern does this require?
3. trace: is that pattern present in the blueprint?
4. if absent: is the absence intentional or an omission?

---

## rule directories i will check

```
.agent/repo=ehmpathy/role=mechanic/briefs/practices/
├── code.prod/
│   ├── pitofsuccess.errors/    ← fail-fast, error paths
│   ├── pitofsuccess.typedefs/  ← type safety
│   ├── pitofsuccess.procedures/← idempotency, validation
│   ├── evolvable.procedures/   ← input-context, DI
│   └── readable.comments/      ← .what/.why
└── code.test/
    └── frames.behavior/        ← given-when-then, coverage
```

---

## coverage gap search: production code

### gap search: error handle

**relevant rule:** rule.require.fail-fast

**question:** does blueprint cover error scenarios?

**check:** criteria usecase.4 (error boundaries) was explicitly EXCLUDED per YAGNI review

**but:** basic fail-fast should still apply for unexpected states

**blueprint codepath:**
```
├─ [+] check for tool_calls in response
│      ├─ if tool_calls: extract invocations, set output null
│      └─ if no tool_calls: parse content as before
```

**gap analysis:**
- what if `response.choices[0]` is undefined? → extant code uses optional chain (`?.`)
- what if `tool_calls` is malformed array? → standard parse error
- what if conversion to openai format fails? → standard error propagation

**found:** no explicit error handle in blueprint, but:
1. usecase.4 excluded per YAGNI
2. extant code uses optional chain
3. standard error propagation is sufficient for v1

**verdict:** NO GAP — error handle covered by YAGNI exclusion + extant patterns

---

### gap search: validation

**relevant rule:** rule.require.fail-fast (validation aspect)

**question:** should blueprint validate `plugs.tools` input?

**check:** rhachet `BrainPlugToolDefinition` is typed. TypeScript provides compile-time validation.

**check:** zod `z.toJSONSchema()` will throw on invalid schema

**found:** input validation is implicit via:
1. TypeScript types on `BrainPlugToolDefinition`
2. zod schema conversion throws on invalid input

**verdict:** NO GAP — validation covered by type system + zod

---

### gap search: .what/.why comments

**relevant rule:** rule.require.what-why-headers

**question:** does blueprint specify comment updates?

**check:** blueprint does not specify .what/.why updates

**but:** extant `ask()` already has .what/.why:
```ts
/**
 * .what = stateless inference (no tool use)
 * .why = provides direct model access for tasks
 */
```

**found:** .what needs update from "no tool use" to "optional tool use"

**should blueprint specify this?**
- blueprints focus on structural changes
- comment updates are implementation details
- documented in has-role-standards-adherance review as "minor issue"

**verdict:** NO GAP — implementation detail, not structural omission

---

### gap search: dependency injection

**relevant rule:** rule.require.dependency-injection

**question:** does blueprint introduce new dependencies that need injection?

**check:** blueprint uses:
- extant `openai` client from context
- extant `z.toJSONSchema()` utility
- rhachet types (imported, not injected)

**found:** no new dependencies introduced. extends extant openai client usage.

**verdict:** NO GAP — no new dependencies

---

## coverage gap search: test code

### gap search: test cases for each criterion

**relevant rules:**
- rule.require.given-when-then
- rule.require.useThen-useWhen-for-shared-results

**question:** does blueprint cover all in-scope criteria with test cases?

**criteria in scope:**
| criteria | blueprint test case | covered? |
|----------|---------------------|----------|
| usecase.1: tool invocation with output schema | "tool invocation with output schema" | YES |
| usecase.2: tool invocation without output schema | "tool invocation without output schema" | YES |
| usecase.1: brain answers directly | "brain answers directly" | YES |
| usecase.5: metrics with tools | "metrics with tools" | YES |

**criteria out of scope:**
| criteria | covered? | reason |
|----------|----------|--------|
| usecase.3: multiple tools | NO | YAGNI excluded |
| usecase.4: error boundaries | NO | YAGNI excluded |

**verdict:** NO GAP — all in-scope criteria have test cases

---

### gap search: test assertion completeness

**question:** do test assertions cover all criterion assertions?

**usecase.1 assertions (8 total):**
| criterion assertion | blueprint test | covered? |
|--------------------|----------------|----------|
| calls.tools contains invocation | "calls.tools populated" | YES |
| slug matches tool definition | "slug present" | YES* |
| input contains parsed arguments | "input present" | YES* |
| exid contains provider ID | "exid present" | YES |
| output is null | "output null" | YES |
| calls is null when direct | "brain answers directly: calls null" | YES |
| output conforms to schema | "brain answers directly: output populated" | YES |
| metrics tracks usage/cost | "metrics with tools" | YES |

*note: "present" vs "matches" — discussed in adherance review. implementation will verify values.

**usecase.2 assertions (4 total):**
| criterion assertion | blueprint test | covered? |
|--------------------|----------------|----------|
| calls.tools contains invocation | "calls.tools populated" | YES |
| output is null | "output null" | YES |
| calls is null when direct | shared with usecase.1 | YES |
| output contains response | shared with usecase.1 | YES |

**usecase.5 assertions (4 total):**
| criterion assertion | blueprint test | covered? |
|--------------------|----------------|----------|
| tokens input tracked | "tokens present" | YES |
| tokens output tracked | "tokens present" | YES |
| cost cash tracked | "cost present" | YES |
| cost time tracked | "time present" | YES |

**verdict:** NO GAP — all criterion assertions have test assertions

---

### gap search: test setup

**question:** does blueprint specify test setup requirements?

**check:** extant tests use:
- `genBrainAtom({ slug: 'xai/grok/code-fast-1' })` — fast model
- `XAI_API_KEY` environment variable
- `jest.setTimeout(30000)` — long timeout for API calls

**found:** blueprint test cases follow same setup pattern. no new setup required.

**verdict:** NO GAP — extant setup sufficient

---

### gap search: test cleanup

**question:** does blueprint specify cleanup?

**check:** extant tests have no cleanup (stateless API calls)

**found:** tool use tests are also stateless. no cleanup needed.

**verdict:** NO GAP — no cleanup required

---

## coverage gap search: packages and contracts

### gap search: rhachet types

**relevant rule:** consistent.contracts

**question:** does blueprint reference all needed rhachet types?

**blueprint contracts:**
- `BrainPlugToolDefinition` — tool input ✓
- `BrainPlugToolInvocation` — tool output ✓
- `BrainOutput` — return type ✓

**check:** are there other rhachet types needed?

checked rhachet exports:
- `BrainAtomPlugs` — already used in extant `ask()` signature
- `BrainPlugToolDefinition` — referenced ✓
- `BrainPlugToolInvocation` — referenced ✓

**verdict:** NO GAP — all needed rhachet types referenced

---

### gap search: openai SDK types

**question:** does blueprint reference openai SDK types for tools?

**check:** openai SDK has:
- `ChatCompletionTool` — tool definition type
- `ChatCompletionMessageToolCall` — tool call response type

**found:** blueprint says "convert plugs.tools to openai format" which implies use of these types

**should blueprint explicitly list these?**
- blueprint contracts section focuses on rhachet types
- openai SDK types are implementation details
- conversion logic will naturally use correct types

**verdict:** NO GAP — openai types are implementation detail, not blueprint-level

---

## patterns that SHOULD be present

### required patterns checklist

| pattern | required for | present in blueprint? |
|---------|-------------|----------------------|
| (input, context) signature | all functions | YES (extends extant) |
| arrow function | all procedures | YES (extends extant) |
| early return | conditional flows | YES (codepath structure) |
| .what/.why comments | procedures | YES (extant + minor update) |
| type safety | all code | YES (rhachet types) |
| given/when/then | integration tests | YES (test coverage) |
| useThen | shared API calls | YES (implied by test structure) |
| fail-fast | error states | YES (YAGNI excluded + extant patterns) |

---

## patterns that are absent (intentionally)

| pattern | why absent |
|---------|-----------|
| error boundary tests | YAGNI excluded (usecase.4) |
| parallel tool tests | YAGNI excluded (usecase.3) |
| helper extraction | WET principle (inline) |
| schema cache | no optimization before needed |
| new domain objects | extends extant types |

---

## conclusion

**no coverage gaps found.**

verified all required patterns are present:
- input/context signature via extant ask() ✓
- arrow function via extant ask() ✓
- early return via codepath structure ✓
- comments via extant + minor update ✓
- type safety via rhachet types ✓
- given/when/then via test structure ✓
- useThen via test assertions ✓
- fail-fast via YAGNI + extant patterns ✓

verified all criterion assertions have test coverage.

verified all needed rhachet types are referenced.

absent patterns are intentionally absent per YAGNI or WET principles.

**no junior omissions detected. blueprint has complete standard coverage.**

