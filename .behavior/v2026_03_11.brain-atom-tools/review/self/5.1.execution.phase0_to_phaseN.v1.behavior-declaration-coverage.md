# review: behavior-declaration-coverage

## summary

all scoped behavior requirements are covered. explicitly excluded usecases documented. one intentional file name change from blueprint.

## artifacts reviewed

- `.behavior/v2026_03_11.brain-atom-tools/1.vision.md` (vision)
- `.behavior/v2026_03_11.brain-atom-tools/2.1.criteria.blackbox.md` (criteria)
- `.behavior/v2026_03_11.brain-atom-tools/3.3.1.blueprint.product.v1.i1.md` (blueprint)
- `src/domain.operations/atom/genBrainAtom.ts` (production code)
- `src/domain.operations/atom/genBrainAtom.integration.test.ts` (test code)
- `src/infra/cast/castIntoXaiToolDef.ts` (cast operation)
- `src/infra/cast/castFromXaiToolCall.ts` (cast operation)
- `src/infra/cast/castIntoXaiToolMessages.ts` (cast operation)

## blueprint filediff tree verification

| blueprint spec | actual file | status | notes |
|----------------|-------------|--------|-------|
| `[~] genBrainAtom.ts` | exists | yes | tool support added at lines 80-82, 98-106, 128-131 |
| `[~] genBrainAtom.integration.test.ts` | exists | yes | cases 4-10 added for tool tests |
| `[+] castIntoXaiToolDef.ts` | exists | yes | 21 lines |
| `[+] castIntoXaiToolDefMessages.ts` | `castIntoXaiToolMessages.ts` | RENAMED | user requested simpler name |
| `[+] castFromXaiToolCall.ts` | exists | yes | 17 lines |

### intentional deviation

**blueprint:** `castIntoXaiToolDefMessages.ts`
**actual:** `castIntoXaiToolMessages.ts`
**reason:** user explicitly requested "not ToolDefMessages, just ToolMessages" per prior conversation

## blueprint codepath verification

### genBrainAtom.ts ask() changes

| codepath | line(s) | status | verification |
|----------|---------|--------|--------------|
| `[+] convert plugs.tools via castIntoXaiToolDef` | 80-82 | yes | `.map((tool) => castIntoXaiToolDef({ tool }))` |
| `[+] detect array vs string prompt` | 97 | yes | `const promptIsToolExecutions = Array.isArray(askInput.prompt)` |
| `[+] castIntoXaiToolMessages for executions` | 99-102 | yes | `castIntoXaiToolMessages({ executions })` |
| `[+] tools parameter in api call` | 116 | yes | `...(tools ? { tools } : {})` |
| `[+] extract tool_calls via castFromXaiToolCall` | 128-131 | yes | `toolCalls.map((call) => castFromXaiToolCall({ call }))` |
| `[+] extract output independently` | 136-140 | yes | `content.length === 0 ? null : askInput.schema.output.parse(...)` |
| `[+] output and calls NOT mutually exclusive` | n/a | yes | independent extraction, both can be non-null |

### cast operations verification

| operation | input type | output type | maps | status |
|-----------|------------|-------------|------|--------|
| `castIntoXaiToolDef` | `BrainPlugToolDefinition` | `OpenAI.ChatCompletionTool` | slug to name, description, schema.input via z.toJSONSchema | yes |
| `castFromXaiToolCall` | `OpenAI.ChatCompletionMessageToolCall` | `BrainPlugToolInvocation` | id to exid, function.name to slug, function.arguments to input | yes |
| `castIntoXaiToolMessages` | `BrainPlugToolExecution[]` | `OpenAI.ChatCompletionMessageParam[]` | assistant tool_calls + tool messages | yes |

## coverage matrix

### usecase.1 = tool invocation with output schema

| criterion | test case | assertion | covered? |
|-----------|-----------|-----------|----------|
| `result.calls.tools contains the tool invocation` | case4 | "calls.tools is populated" | yes |
| `result.calls.tools[].slug matches the tool definition slug` | case4 | "invocation has slug" | yes |
| `result.calls.tools[].input contains parsed arguments` | case4 | "invocation has input" | yes |
| `result.calls.tools[].exid contains provider correlation ID` | case4 | "invocation has exid" | yes |
| `result.output is null` | case4 | "output is null when tools called" | yes |
| (direct answer) `result.calls is null` | case6 | "calls is null" | yes |
| (direct answer) `result.output conforms to the output schema` | case6 | "output is populated" | yes |
| (direct answer) `result.metrics tracks token usage and cost` | case2 | "metrics includes token counts/cash costs" | yes |

### usecase.2 = tool invocation without output schema

| criterion | test case | assertion | covered? |
|-----------|-----------|-----------|----------|
| `result.calls.tools contains the tool invocation` | case5 | "calls.tools is populated" | yes |
| `result.output is null` | case5 | "output is null" | yes |
| (direct answer) `result.calls is null` | case6 | "calls is null" | yes |
| (direct answer) `result.output contains the response` | case6 | "output is populated" | yes |

### usecase.3 = multiple tools plugged

| criterion | status | rationale |
|-----------|--------|-----------|
| single tool request | case4, case5 | implicitly covered via `length > 0` assertions |
| parallel tool requests | EXCLUDED | per vision: "out of scope for this work" |

### usecase.4 = error boundaries

| criterion | status | rationale |
|-----------|--------|-----------|
| invalid tool schema | EXCLUDED | per YAGNI review: wish focuses on success path |
| malformed provider response | EXCLUDED | per YAGNI review: standard parse errors sufficient |

rationale: documented in `.behavior/review/self/3.3.1.blueprint.product.v1.has-pruned-yagni.md`

### usecase.5 = metrics with tools

| criterion | test case | assertion | covered? |
|-----------|-----------|-----------|----------|
| `result.metrics.size.tokens tracks input tokens` | case7 | "tokens input is tracked" | yes |
| `result.metrics.size.tokens tracks output tokens` | case7 | "tokens output is tracked" | yes |
| `result.metrics.cost.cash tracks monetary cost` | case7 | "cost cash is tracked" | yes |
| `result.metrics.cost.time tracks elapsed milliseconds` | case7 | "cost time is tracked" | yes |

### blueprint test coverage (additional)

| test case | blueprint requirement | covered? |
|-----------|----------------------|----------|
| case8 | tool continuation (success signal) | yes |
| case9 | error:constraint signal (BadRequestError) | yes |
| case10 | error:malfunction signal (UnexpectedCodePathError) | yes |

## conclusion

all scoped requirements are covered:
- usecase.1: 8/8 criteria covered
- usecase.2: 4/4 criteria covered
- usecase.3: single tool covered, parallel explicitly excluded
- usecase.4: explicitly excluded per YAGNI review
- usecase.5: 4/4 criteria covered
- blueprint signal tests: case8, case9, case10 covered
