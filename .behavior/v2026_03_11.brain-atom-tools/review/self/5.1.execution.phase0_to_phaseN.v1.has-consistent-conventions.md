# review: has-consistent-conventions

## summary

all name conventions are consistent with extant patterns. no divergence found.

## artifacts reviewed

- `src/infra/cast/castIntoXaiToolDef.ts`
- `src/infra/cast/castFromXaiToolCall.ts`
- `src/infra/cast/castIntoXaiToolMessages.ts`
- `src/domain.operations/atom/genBrainAtom.ts` (new parameters and types)

## review

### directory structure conventions

| pattern | extant example | new code | consistent? |
|---------|----------------|----------|-------------|
| `domain.operations/{entity}/` | `domain.operations/atom/` | (no new dirs) | n/a |
| `infra/{purpose}/` | (none prior) | `infra/cast/` | yes, follows ehmpathy pattern |
| `contract/sdk/` | extant | (no changes) | n/a |

### file name conventions

| pattern | extant example | new code | consistent? |
|---------|----------------|----------|-------------|
| `gen{Entity}.ts` | `genBrainAtom.ts` | (no new gen files) | n/a |
| `{Entity}.config.ts` | `BrainAtom.config.ts` | (no new config files) | n/a |
| `cast{Direction}{Target}.ts` | (none prior) | `castIntoXaiToolDef.ts`, `castFromXaiToolCall.ts`, `castIntoXaiToolMessages.ts` | yes, follows cross-repo pattern |

### function name conventions

| pattern | extant example | new code | consistent? |
|---------|----------------|----------|-------------|
| `gen{Entity}` | `genBrainAtom` | (no new gen functions) | n/a |
| `get{Entity}sBy{Provider}` | `getBrainAtomsByXAI` | (no new get functions) | n/a |
| `cast{Into\|From}{Provider}{Target}` | (none prior) | `castIntoXaiToolDef`, `castFromXaiToolCall`, `castIntoXaiToolMessages` | yes |

### type name conventions

| pattern | extant example | new code | consistent? |
|---------|----------------|----------|-------------|
| `{Provider}{Entity}{Role}` | `XaiBrainAtomSlug` | (no new type exports) | n/a |
| `{Entity}{Role}` | `BrainAtomConfig` | (no new type exports) | n/a |

### parameter name conventions

| pattern | extant example | new code | consistent? |
|---------|----------------|----------|-------------|
| `askInput.plugs` | rhachet BrainAtom contract | `askInput.plugs?.tools` | yes |
| `askInput.prompt` | extant in genBrainAtom | `askInput.prompt: string \| BrainPlugToolExecution[]` | yes, union extension |
| `askInput.schema.output` | extant in genBrainAtom | (unchanged) | yes |

### variable name conventions in genBrainAtom.ts

| pattern | extant example | new code | consistent? |
|---------|----------------|----------|-------------|
| `{entity}s` for arrays | `messages`, `exchanges` | `tools`, `invocations`, `executions` | yes |
| `camelCase` for vars | `systemPrompt`, `jsonSchema` | `toolCalls`, `promptIsToolExecutions` | yes |
| descriptive booleans | (limited prior examples) | `promptIsToolExecutions` | yes |

### xai capitalization conventions

| context | convention | new code | consistent? |
|---------|------------|----------|-------------|
| slug values | `xai` lowercase | n/a (no new slugs) | n/a |
| environment variables | `XAI_API_KEY` uppercase | n/a (no new env vars) | n/a |
| function names | `getBrainAtomsByXAI` uppercase suffix | n/a (no new XAI suffix functions) | n/a |
| type/function names with Xai prefix | `XaiBrainAtomSlug` title case | `castIntoXaiToolDef`, `castFromXaiToolCall`, `castIntoXaiToolMessages` | yes, uses `Xai` title case |

### abbreviation conventions

| pattern | extant example | new code | consistent? |
|---------|----------------|----------|-------------|
| `Config` not `Configuration` | `BrainAtomConfig` | n/a | n/a |
| `Spec` not `Specification` | `BrainSpec` | n/a | n/a |
| `Def` not `Definition` | (none prior) | `castIntoXaiToolDef` | yes, follows abbreviation pattern |

note: rhachet uses `BrainPlugToolDefinition` (full word) but our function targets the xai output format, not the rhachet input. abbreviations are consistent with extant `Config`, `Spec` patterns.

## conclusion

all conventions are consistent with extant patterns:
- cast operations follow `cast{Into|From}{Provider}{Target}` pattern
- `Xai` title case consistent with `XaiBrainAtomSlug` type convention
- `Def` abbreviation consistent with `Config`, `Spec` abbreviation pattern
- file placement in `src/infra/cast/` follows ehmpathy infra layer pattern
- parameter and variable names follow extant camelCase conventions
- no new terms introduced where extant terms exist
