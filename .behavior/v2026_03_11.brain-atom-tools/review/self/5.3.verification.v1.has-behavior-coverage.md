# review: has-behavior-coverage

## summary

all behaviors from wish and vision are covered by tests.

## wish coverage

the wish states:
> we need to cover with integration and acceptance tests that tools are successfully invocable w/ and w/o output schema

| wish behavior | test case | location | covered? |
|---------------|-----------|----------|----------|
| tools successfully invocable | case4, case5, case8 | genBrainAtom.integration.test.ts | yes |
| w/ output schema | case4 | "tool invocation with output schema" | yes |
| w/o output schema | case5 | "tool invocation without output schema validation" | yes |

## vision coverage

from vision "usecases and goals (scoped to wish)":

| usecase | test case | covered? |
|---------|-----------|----------|
| 1. tool invocation with output schema | case4 | yes |
| 2. tool invocation without output schema | case5 | yes |
| 3. tool continuation | case8 | yes |

from vision "evaluation" criteria:

| criterion | test case | covered? |
|-----------|-----------|----------|
| tools invocable | case4, case5 | yes |
| w/ output schema | case4 | yes |
| w/o output schema | case5 | yes |
| tool continuation | case8 | yes |
| type safety | compile-time (typescript) | yes |

## additional coverage (beyond wish scope)

the implementation includes signal tests for error cases:

| extra test | case | why included |
|------------|------|--------------|
| error:constraint signal | case9 | vision mentions signal classification |
| error:malfunction signal | case10 | vision mentions signal classification |
| metrics with tools | case7 | vision "observable" criterion |
| brain answers directly | case6 | vision "direct answer" edgecase |

## test file location

all tool-related tests are in:
- `src/domain.operations/atom/genBrainAtom.integration.test.ts`
- cases 4-10 (7 test cases total)

## why coverage holds

1. **wish explicitly scopes to tool invocation w/ and w/o output schema** — case4 and case5 cover these exactly
2. **vision scopes tool continuation** — case8 covers complete loop (ask → tool call → execute → continue → output)
3. **signal tests exceed scope** — cases 9-10 test error classification, not required by wish but good coverage
4. **all tests pass** — 44/44 tests pass in integration suite
