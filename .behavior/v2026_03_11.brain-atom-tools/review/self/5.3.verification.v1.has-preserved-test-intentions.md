# review: has-preserved-test-intentions

## summary

all prior test intentions preserved. new tests added. no assertions weakened.

## prior tests (case1-case3)

the extant test file had 3 test cases before this behavior:

| case | intent | touched? | preserved? |
|------|--------|----------|------------|
| case1 | verify atom creation (repo, slug, description) | no | yes |
| case2 | verify ask() with simple prompt and briefs | no | yes |
| case3 | verify episode continuation | no | yes |

### why preserved

case1-case3 were not modified. diff shows only additions:
- case4-case10 added (new functionality)
- no changes to case1-case3 assertions
- no changes to case1-case3 expected values

## new tests (case4-case10)

new tests added for tool use functionality:

| case | intent | why added |
|------|--------|-----------|
| case4 | tool invocation with output schema | wish requirement |
| case5 | tool invocation without output schema | wish requirement |
| case6 | brain answers directly with tools | edgecase from vision |
| case7 | metrics with tools | vision "observable" criterion |
| case8 | tool continuation | vision "tool continuation" usecase |
| case9 | error:constraint signal | vision signal classification |
| case10 | error:malfunction signal | vision signal classification |

### why intentions are clear

each new test has clear intent:
1. test name declares what is verified
2. given/when/then structure makes flow explicit
3. assertions check specific properties, not just "it works"

## forbidden patterns check

| forbidden pattern | found? | evidence |
|-------------------|--------|----------|
| weakened assertions | no | all assertions use explicit values |
| removed test cases | no | case1-case3 unchanged, diff shows additions only |
| changed expected values | no | no edits to prior case expected values |
| deleted failed tests | no | no tests deleted |

## code changes to test file

examined git diff for test file changes:

```
git diff main -- src/domain.operations/atom/genBrainAtom.integration.test.ts
```

changes are additive:
- line 147+: added case4 (tool invocation with output schema)
- line 200+: added case5 (tool invocation without output schema)
- line 230+: added case6 (brain answers directly)
- line 257+: added case7 (metrics with tools)
- line 292+: added case8 (tool continuation)
- line 346+: added case9 (error:constraint signal)
- line 394+: added case10 (error:malfunction signal)

no modifications to lines 1-146 (extant test cases).

## why intentions are preserved

1. **no test code deleted** — all prior tests still exist in test file
2. **no assertions changed** — prior expected values unchanged
3. **no test behavior weakened** — assertions remain strict
4. **new tests extend** — case4-10 add coverage, not replace it
