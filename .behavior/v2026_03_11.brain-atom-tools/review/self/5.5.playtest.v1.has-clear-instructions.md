# review: has-clear-instructions

## summary

all instructions are followable. commands copy-pasteable. outcomes explicit.

reviewed each path against the three criteria. walked through as if first time in repo.

## methodology

1. opened `5.5.playtest.v1.i1.md` in full
2. for each path, asked: "could i run this with no codebase knowledge?"
3. for each command, asked: "can i copy-paste this directly?"
4. for each outcome, asked: "would i know if it passed or failed?"

## followable without prior context?

| path | followable? | evidence |
|------|-------------|----------|
| path 1 | yes | source apikeys, run test:integration with pattern |
| path 2 | yes | single npm command |
| path 3 | yes | single npm command |
| path 4 | yes | single npm command |
| path 5 | yes | single npm command |
| edge 1 | yes | single npm command |
| edge 2 | yes | single npm command |
| edge 3 | yes | single npm command |

### why followable

1. **prerequisites listed** — node.js, api key source, pnpm install
2. **each path is self-contained** — no dependencies between paths
3. **no assumed knowledge** — commands are explicit npm scripts

## commands copy-pasteable?

| command | copy-pasteable? | evidence |
|---------|-----------------|----------|
| `source .agent/repo=.this/role=any/skills/use.apikeys.sh` | yes | full path |
| `npm run test:integration -- --testNamePattern="case4"` | yes | exact pattern |
| `npm run test:integration -- genBrainAtom.integration.test.ts` | yes | exact filename |

### why copy-pasteable

1. **no placeholders** — no `$VAR` or `<replace>` markers
2. **full paths** — api key source uses full relative path
3. **exact patterns** — test patterns use exact case names

## expected outcomes explicit?

| path | outcome explicit? | evidence |
|------|-------------------|----------|
| path 1 | yes | "test passes", "result.calls.tools is defined", "result.output is null" |
| path 2 | yes | "test passes", "result.calls.tools contains invocation with slug, input, exid" |
| path 3 | yes | "test passes", "result.calls is null", "result.output contains the answer" |
| path 4 | yes | "test passes", "first ask returns calls.tools", "continuation produces result.output" |
| path 5 | yes | "test passes", specific metric properties > 0 |
| edge 1 | yes | "test passes", "brain produces output that explains the constraint violation" |
| edge 2 | yes | "test passes", "brain produces output that explains the service failure" |
| edge 3 | yes | "all 44 tests pass", "no test skips", "no flaky failures" |

### why explicit

1. **specific properties named** — e.g., `metrics.size.tokens.input > 0`
2. **behavior described** — e.g., "brain deferred to tool"
3. **counts given** — e.g., "44/44 tests passed"

## issues found and fixed

none. the playtest was authored with these criteria in mind.

## what makes this playtest work

### self-contained paths

each path is independent. path 3 does not depend on path 2. a foreman can run any single path without setup from prior paths.

**evidence:** no path says "after completing path 2, ..." — each starts fresh.

### explicit test patterns

the commands use `--testNamePattern="caseN"` which maps directly to test names in the test file. no guesswork required.

**evidence:** `case4`, `case5`, etc. are the actual test labels in `genBrainAtom.integration.test.ts`.

### observable outcomes

each expected outcome references specific properties: `result.calls.tools`, `metrics.size.tokens.input`, etc. these are observable in test output or debugger.

**evidence:** path 5 lists four specific metric properties rather than "metrics are tracked".

## conclusion

instructions are clear:
- ✅ followable without prior context — each path is self-contained
- ✅ commands copy-pasteable — full paths, exact patterns, no placeholders
- ✅ expected outcomes explicit — specific properties named, not vague "works"

