# review: has-vision-coverage

## summary

all wish and vision behaviors are covered by playtest paths.

walked through 0.wish.md and 1.vision.md line by line. mapped each requirement to a playtest path.

## methodology

1. opened `0.wish.md` and extracted each testable requirement
2. opened `1.vision.md` and extracted usecases, evaluation criteria, edgecases
3. for each requirement, found the playtest path that verifies it
4. marked out-of-scope items with clear reason

## wish coverage

### source review (0.wish.md)

the wish states (lines 13-15):

```
we need to cover with integration and acceptance tests that tools are successfully invocable

w/ and w/o output schema
```

### extraction

| line | requirement extracted |
|------|----------------------|
| 13 | tools successfully invocable |
| 15 | w/ output schema |
| 15 | w/o output schema |

### map to playtest

| wish requirement | playtest path | covered? | evidence |
|------------------|---------------|----------|----------|
| tools successfully invocable | path 1, path 2, path 4 | yes | case4, case5, case8 test tool invocation |
| w/ output schema | path 1 | yes | case4 tests with response_format |
| w/o output schema | path 2 | yes | case5 tests without response_format |

### why this map holds

1. **path 1 tests "w/ output schema"** because case4 uses both `plugs.tools` AND `schema.output` with provider-validated JSON.

2. **path 2 tests "w/o output schema"** because case5 tests tools WITHOUT `response_format` sent to xai (but still has rhachet schema for client-side parse).

3. **path 4 tests "successfully invocable"** because it runs the full loop: ask → tool call → execute → continue → output.

## vision coverage

from `1.vision.md` usecases:

| usecase | playtest path | covered? |
|---------|---------------|----------|
| 1. tool invocation with output schema | path 1 | yes |
| 2. tool invocation without output schema | path 2 | yes |
| 3. tool continuation | path 4 | yes |

from `1.vision.md` evaluation criteria:

| criterion | playtest path | covered? |
|-----------|---------------|----------|
| tools invocable | path 1, path 2 | yes |
| w/ output schema | path 1 | yes |
| w/o output schema | path 2 | yes |
| tool continuation | path 4 | yes |
| type safety | path 3 (compile-time) | yes |
| observable (metrics) | path 5 | yes |

from `1.vision.md` pit-of-success edgecases:

| edgecase | playtest path | covered? |
|----------|---------------|----------|
| tools plugged, brain answers directly | path 3 | yes |
| brain requests unknown tool slug | out of scope (atom returns, caller validates) | n/a |
| multiple parallel tool calls | out of scope per wish | n/a |
| no tools plugged | extant tests (case1-3) | yes |
| tool execution error (constraint) | edge 1 | yes |
| tool execution error (malfunction) | edge 2 | yes |

## requirements left untested?

| requirement | status | reason |
|-------------|--------|--------|
| parallel tool calls | out of scope | wish did not include this |
| unknown tool slug | out of scope | atom returns slug, caller validates |

all in-scope requirements are tested.

## issues found and fixed

none. the playtest paths map directly to wish and vision requirements.

## why coverage is complete

### wish is narrow and explicit

the wish says exactly:
> we need to cover with integration and acceptance tests that tools are successfully invocable w/ and w/o output schema

this maps to two specific requirements:
1. invocable with output schema → path 1
2. invocable without output schema → path 2

no ambiguity. no hidden requirements.

### vision usecases are enumerated

the vision lists three usecases explicitly numbered:
1. tool invocation with output schema → path 1
2. tool invocation without output schema → path 2
3. tool continuation → path 4

each has a 1:1 map to a playtest path.

### out-of-scope items are documented

the review explicitly calls out:
- parallel tool calls → out of scope per wish
- unknown tool slug → atom returns slug, caller validates

this prevents false coverage claims.

## conclusion

playtest covers all behaviors:
- ✅ wish requirements covered (invocable w/ and w/o output schema)
- ✅ vision usecases covered (1, 2, 3)
- ✅ vision evaluation criteria covered
- ✅ pit-of-success edgecases covered (where in scope)
- ✅ out-of-scope items documented with reason

