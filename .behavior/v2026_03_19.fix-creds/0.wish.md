wish =

we want to support a context passed in on genBrainAtom

e.g., a `ContextBrainSupplier<'xai', BrainSuppliesXai> = { ['brain.supplier.xai']: { creds: { XAI_API_KEY } } }`

(where ContextBrainSupplier we will eventually lift up into rhachet itself)

and then source the creds from the supplier context as first option, and only fallback to env-var if not supplied

---

or even better, make the creds a getter, so we're not passing around creds in plaintext all over


e.g., 


{ creds: () => Promise<{ XAI_API_KEY: string }> }
