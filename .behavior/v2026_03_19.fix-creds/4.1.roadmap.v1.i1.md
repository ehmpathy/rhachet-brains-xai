# 4.1.roadmap: credential supplier implementation

## execution phases

### phase 0: types

**read first:**
- `.behavior/v2026_03_19.fix-creds/3.3.1.blueprint.product.v1.i1.md` (type declarations section)

**tasks:**
- [ ] create `src/_topublish/rhachet/ContextBrainSupplier.ts`
  - generic type for brain suppliers
  - JSDoc with .what/.why/.note
- [ ] add `BrainSuppliesXai` type to `src/domain.operations/atom/BrainAtom.config.ts`
  - type declaration with creds getter

**acceptance:**
- types compile without error
- `npm run test:types` passes

---

### phase 1: domain operation

**depends on:** phase 0

**read first:**
- `.behavior/v2026_03_19.fix-creds/3.3.1.blueprint.product.v1.i1.md` (getSdkXaiCreds section)

**tasks:**
- [ ] create `src/domain.operations/creds/getSdkXaiCreds.ts`
  - signature: `(input: Empty, context?: {...})`
  - extracts supplier from context
  - calls getter if present
  - falls back to env var
  - fail-fast on absent credentials
- [ ] create `src/domain.operations/creds/getSdkXaiCreds.test.ts`
  - case 1: context with supplier calls getter
  - case 2: no context falls back to env var
  - case 3: getter returns undefined throws
  - case 4: no supplier and no env var throws

**acceptance:**
- `npm run test:unit -- getSdkXaiCreds.test.ts` passes
- all 4 test cases pass

---

### phase 2: integration

**depends on:** phase 1

**read first:**
- `.behavior/v2026_03_19.fix-creds/3.3.1.blueprint.product.v1.i1.md` (genBrainAtom section)
- `.behavior/v2026_03_19.fix-creds/3.1.3.research.internal.product.code.prod._.v1.stone` (if declared)

**tasks:**
- [ ] update `src/domain.operations/atom/genBrainAtom.ts`
  - import getSdkXaiCreds
  - import ContextBrainSupplier, BrainSuppliesXai
  - re-export BrainSuppliesXai
  - extend context type to accept supplier
  - replace inline openai creation with getSdkXaiCreds call
- [ ] update `src/contract/sdk/index.ts`
  - export BrainSuppliesXai

**acceptance:**
- `npm run test:types` passes
- extant `npm run test:integration -- genBrainAtom.integration.test.ts` passes (env var path)

---

### phase 3: credential tests

**depends on:** phase 2

**read first:**
- `.behavior/v2026_03_19.fix-creds/2.1.criteria.blackbox.md` (usecases 2,3,4,6)
- `.behavior/v2026_03_19.fix-creds/3.1.3.research.internal.product.code.test._.v1.stone` (if declared)

**tasks:**
- [ ] create `src/domain.operations/atom/genBrainAtom.credentials.integration.test.ts`
  - case 1: context with creds getter succeeds
  - case 2: getter called fresh per ask
  - case 3: getter error propagates
  - case 4: multi-tenant isolation

**acceptance:**
- `npm run test:integration -- genBrainAtom.credentials.integration.test.ts` passes
- all 4 test cases pass

---

### phase 4: documentation

**depends on:** phase 3

**read first:**
- `.behavior/v2026_03_19.fix-creds/1.vision.md` (documentation requirements section)

**tasks:**
- [ ] update `README.md`
  - add "credential supplier" section
  - usecase 1: simple local development
  - usecase 2: production with secrets manager
  - usecase 3: multi-tenant credential isolation
  - usecase 4: tests without env pollution
  - cache recommendation with `with-simple-cache`

**acceptance:**
- README contains all 4 usecases
- code examples compile

---

## verification

after all phases complete:

- [ ] `npm run test:types` passes
- [ ] `npm run test:lint` passes
- [ ] `npm run test:unit` passes
- [ ] `npm run test:integration` passes
- [ ] extant behavior unchanged (env var fallback works)
- [ ] new behavior works (credential supplier context)

