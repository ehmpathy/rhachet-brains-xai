# self-review: has-complete-defect-entries

## guide question

> does each defect entry have what, why, fix?
> go through each defect in the artifact and confirm:
> - what: the defect is described
> - why: root cause is documented
> - fix: how it was fixed is documented

## verification process

i opened `3.3.repair.cicd.defects.v1.i1.md` and read through each defect entry line by line. below i quote each field from the artifact and verify completeness.

### defect 1: XAI_API_KEY secret not passed

**artifact quotes**:

```markdown
**what**: integration tests failed with "XAI_API_KEY is required"

**why**: the upgrade introduced test shard support via `.github/workflows/.test.yml`,
which is a reusable workflow. reusable workflows require explicit `secrets:`
input declarations to receive secrets from the caller. the upgrade added the
workflow structure but the secret was not wired through.

**fix**: added `xai-api-key` secret input to `.test.yml` and passed it from
`test.yml` and `publish.yml`:
```

| field | present | analysis |
|-------|---------|----------|
| what | yes | describes error message "XAI_API_KEY is required" |
| why | yes | explains reusable workflows need explicit secrets input |
| fix | yes | shows yaml snippets for both input declaration and caller |

### defect 2: depcheck flags self-reference

**artifact quotes**:

```markdown
**what**: test:lint:deps failed with "Unused devDependencies: rhachet-brains-xai"

**why**: the package references itself in devDependencies (for acceptance tests
that import the published package). depcheck does not recognize self-references
as valid usage.

**fix**: added `rhachet-brains-xai` to `.depcheckrc.yml` ignores list.
```

| field | present | analysis |
|-------|---------|----------|
| what | yes | describes depcheck error message |
| why | yes | explains self-reference pattern depcheck cannot detect |
| fix | yes | specifies file and change made |

### defect 3: null runtime in slow test report

**artifact quotes**:

```markdown
**what**: test-shards-integration job failed on "report slow tests" step with
"null: unbound variable"

**why**: the upgrade added a "report slow tests" step that parses jest results.
when a test suite is skipped, `perfStats.runtime` is `null`. the jq output
includes literal "null" which fails bash arithmetic `$((runtime / 1000))` due
to `set -eu`.

**fix**: added jq `select()` to filter out null runtimes:
```

| field | present | analysis |
|-------|---------|----------|
| what | yes | describes bash error "null: unbound variable" |
| why | yes | explains null runtime from skipped tests + bash arithmetic failure |
| fix | yes | shows jq select() filter code |

## summary

| defect | what | why | fix | verdict |
|--------|------|-----|-----|---------|
| 1 | complete | complete | complete | pass |
| 2 | complete | complete | complete | pass |
| 3 | complete | complete | complete | pass |

## why it holds

1. **verified against artifact**: each quote above comes from the actual artifact text
2. **what fields describe errors**: all three describe the actual error message or failure point
3. **why fields explain root cause**: all three explain what caused the defect (upgrade changes)
4. **fix fields show resolution**: all three describe or show code for the fix applied

## conclusion

all three defect entries have complete what, why, and fix fields. the artifact meets the requirement.
