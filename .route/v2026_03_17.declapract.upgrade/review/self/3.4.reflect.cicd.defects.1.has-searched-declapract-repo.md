# self-review: has-searched-declapract-repo

## guide question

> did we search declapract-typescript-ehmpathy via gh cli for the exact practice?
> for each practice bug, cite the root cause with a link or search result.

## practice bugs identified

**one practice bug**: defect 3 (null runtime in slow test report)

## search verification

### search 1: find the practice file

```sh
gh search code --repo ehmpathy/declapract-typescript-ehmpathy "perfStats.runtime" --limit 5
```

**result**:

```
ehmpathy/declapract-typescript-ehmpathy:src/practices/cicd-common/best-practice/.github/workflows/.test.yml: jq -r '.testResults[] | "\(.name):\(.perfStats.runtime)"' jest-results.json | while IFS=: read -r name runtime; do
ehmpathy/declapract-typescript-ehmpathy:src/practices/cicd-common/best-practice/.github/workflows/.test.yml: jq -r '.testResults[] | "\(.name):\(.perfStats.runtime)"' jest-results.json | while IFS=: read -r name runtime; do
```

**analysis**: the practice file exists and contains the jq command without the select() filter.

### search 2: verify fix is absent

```sh
gh search code --repo ehmpathy/declapract-typescript-ehmpathy "select.*perfStats.runtime" --limit 5
```

**result**: no matches

**analysis**: the select() filter does not exist in the practices repo. this confirms the practice bug - the fix needs to be added.

## verification summary

| search | result | status |
|--------|--------|--------|
| perfStats.runtime | found in .test.yml | confirms practice exists |
| select filter | not found | confirms fix is absent |

## why it holds

1. **searched via gh cli**: two searches against the practices repo
2. **results cited**: command and output documented
3. **root cause confirmed**: the practice lacks the select() filter that handles null runtimes
4. **fix path clear**: src/practices/cicd-common/best-practice/.github/workflows/.test.yml

## conclusion

the practices repo was searched. the practice bug is confirmed with search results cited.
