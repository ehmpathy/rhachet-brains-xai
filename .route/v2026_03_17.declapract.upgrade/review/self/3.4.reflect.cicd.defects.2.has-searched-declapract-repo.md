# self-review: has-searched-declapract-repo

## guide question

> did we search declapract-typescript-ehmpathy via gh cli for the exact practice?
> for each practice bug, cite the root cause with a link or search result.

## verification process

i opened `3.4.reflect.cicd.defects.v1.i1.md` and verified that each practice bug has search commands and results cited.

### practice bugs identified in 3.4 artifact

from the classifications table:

```markdown
| 3: null runtime in slow test report | **practice bug** | the jq command in practices lacks select() filter for null runtimes |
```

**count**: 1 practice bug (defects 1 and 2 are repo quirks, no search needed)

### search commands executed

from the artifact's "search verification" section:

**search 1**: find the practice file

```sh
gh search code --repo ehmpathy/declapract-typescript-ehmpathy "perfStats.runtime" --limit 5
```

**result cited in artifact**:

```
ehmpathy/declapract-typescript-ehmpathy:src/practices/cicd-common/best-practice/.github/workflows/.test.yml: jq -r '.testResults[] | "\(.name):\(.perfStats.runtime)"' ...
```

**search 2**: verify fix is absent

```sh
gh search code --repo ehmpathy/declapract-typescript-ehmpathy "select.*perfStats.runtime" --limit 5
```

**result cited in artifact**: "(no matches)"

### analysis in artifact

the artifact explains:

```markdown
**analysis**: the practice file exists and contains the jq command without the select() filter.
**analysis**: the select() filter does not exist in the practices repo. this confirms the practice bug - the fix needs to be added.
```

## summary

| question | answer |
|----------|--------|
| did we search via gh cli? | yes - 2 commands executed |
| did we cite results? | yes - both outputs documented |
| did we identify root cause? | yes - practice lacks select() filter |
| did we cite file path? | yes - `src/practices/cicd-common/best-practice/.github/workflows/.test.yml` |

## why it holds

1. **searched via gh cli**: both `gh search code` commands are documented
2. **results cited**: output from both searches is quoted in the artifact
3. **root cause identified**: "jq command in practices lacks select() filter for null runtimes"
4. **fix path documented**: exact file in practices repo that needs the fix

## conclusion

the practices repo was searched via gh cli. the practice bug has search results cited and root cause documented.